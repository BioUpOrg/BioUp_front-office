/*import React from 'react';
import "./styles.css"
import { useNavigate } from "react-router-dom";

export function SideBar(){
    const navigate = useNavigate();


    const list = (e) => {
      navigate("/products/list");
      };
      const add = (e) => {
        navigate("/products/add");
      };
      const my = (e) => {
        navigate("/products/myproducts");
      };

        return (
          <div style={{marginRight:'3rem',marginLeft:'1rem',marginTop:'1rem',border:"1px solid #07bc0c",borderRadius:"15px"}}>
              <div className="nav" >
                <a onClick={(e)=>my(e)} style={{fontWeight: 'bold',fontSize:"20px"}}>List my products</a>
                <a onClick={(e)=>add(e)} style={{fontWeight: 'bold',fontSize:"20px"}}>Add new product</a>
                <a onClick={(e)=>list(e)} style={{fontWeight: 'bold',fontSize:"20px"}}>View All products</a>
              </div>
            </div>
        );
    }
      
    
      
      
export default SideBar; */